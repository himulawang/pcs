exports.init = function init() {
    var loader = require(I_ABS_PATH + '/lib/Loader.js').Loader;
    loader.loadCore();
    loader.loadApp(APP_ABS_PATH);
};
/*
var loader = require('./lib/Loader.js').Loader;
var maker = require('./lib/Maker.js').Maker;
var orms = require(APP_ABS_PATH + '/server/config/orm.js').orms;

exports.init = function() {
    // load I Framework core
    loader.loadCore();

    // create base model files in memory
    maker.loadOrms(orms);

    // write model file to disk
    maker.createFiles(orms, APP_ABS_PATH + '/server/model');
    loader.loadDir(APP_ABS_PATH  + '/server/model');

    // load disk classes
    loader.load(APP_ABS_PATH + '/server');

    // load model classes in memory
    loader.loadMemoryModelClasses(maker.getClasses());
};

*/
